namespace {{ namespacebase }}.{{ singular }}UseCases.Create{{ singular }};

public class Create{{ singular }}Model
{
    public required string Name { get; set; }
    {{ render_class_properties properties ~}}
}
